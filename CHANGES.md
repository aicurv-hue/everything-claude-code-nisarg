# Changes

2026-05-16 — feat(team): gate the Team feature behind a "Coming Soon" UI surface, pending LinkedIn MDP approval. Backend (routes, helpers, Firestore schema, cron behavior) is fully intact and unchanged — this is a frontend-only suppression so we can launch the feature later by reverting four files. Four surfaces changed: (1) `/dashboard/settings/team` page replaced (~500 lines → 50 lines) with a centered "Coming Soon" placeholder card explaining the feature is built and waiting on LinkedIn MDP review, with "Back to dashboard" + "Notify me" (mailto) CTAs. (2) `/invite/[token]` route similarly replaced with a "Team invites are paused" placeholder reassuring invitees their invite is safe and the inviter can re-share once we launch — backend invite-token routes (`/api/team/invite`, `accept`, `revoke`, `invite/[token]/preview`) are untouched and still work. (3) `PendingInviteBanner` now returns `null` so existing pending invites in Firestore don't surface a non-actionable banner. (4) Sidebar Team nav item gains a small "Soon" pill so the entry point sets expectations before users click. (5) `PricingCards` Business plan feature row updated from "1 admin + 5 team members" to "1 admin + 5 team members (coming soon)" so we don't oversell on the pricing surface — Business plan still gates Campaigns + Company Page + Unlimited, which are all live. Re-enable path: `git revert` the commit, all UI returns instantly because no backend was touched.

2026-05-16 — fix(brand): landing-page navbar wordmark was a plain non-clickable `<span>Cridl</span>` — replaced with the shared `Logo` component wrapped in `<Link href="/">` so (a) the brand mark renders with the correct "crid" + blue "l" treatment used everywhere else in the app, and (b) clicking the logo navigates home as users expect from every other site. Also applies to /privacy and /terms which reuse HomeNavbar, so the same logo-as-home-link behavior is now consistent across the marketing + legal surface.

2026-05-16 — fix(legal): wire up the new /privacy and /terms pages end-to-end. Two gaps after yesterday's launch: (1) HomeNavbar's "Pricing" and "Get started free" buttons used a bare `#pricing` fragment which is dead on any page other than the landing page — clicking either from /privacy or /terms did nothing. Switched both to `<Link href="/#pricing">` so the navbar consistently routes to the landing page's pricing anchor from anywhere. (2) The landing-page footer at app.cridl.com only carried the copyright line and a stray "app.cridl.com" tag — no path from the marketing surface to the new legal pages. Expanded the footer to include Privacy + Terms + support email alongside the existing copy. Both /privacy and /terms already cross-link to each other in their own footers, so the loop is now complete from any direction.

2026-05-16 — feat(legal): public `/privacy` and `/terms` pages. LinkedIn MDP application requires both URLs to be live and reachable on the production domain before reviewers will read the application copy — they were missing. New routes at `src/app/privacy/page.tsx` and `src/app/terms/page.tsx`, each a single-file public page extending the root layout (no auth), reusing `HomeNavbar` and the landing-page slate/white palette for consistency. Privacy covers all 12 items LinkedIn scans for: who we are, what we collect (account / LinkedIn / content / payment / telemetry), lawful basis under DPDP & GDPR, where it's stored (Firestore asia-south1, Vercel us-east), explicit sub-processor list (Firebase, Vercel, OpenRouter, fal.ai, Razorpay, LinkedIn), LinkedIn-specific handling (server-side tokens, explicit publish only, revoke paths), retention (30-day post-deletion + 90-day backup), user rights, cookies (minimal — Firebase Auth + 10-min OAuth state), no-children clause, change-notification clause, contact + India governing law. Terms covers the standard 15 items: acceptance, service description with explicit LinkedIn non-affiliation, eligibility (18+), account responsibility, acceptable use (incl. LinkedIn ToS pass-through), Razorpay billing & no partial refunds, AI-content responsibility shift to user, LinkedIn-dependency disclaimer (explicit MDP-restriction call-out), IP, "as is" warranty disclaimer, 12-month-fees liability cap, termination, change notification, India governing law, contact. Both pages drop a minimal footer with mutual cross-links and `support@cridl.app`. No footer plumbing on the landing page or other in-app surfaces — LinkedIn reaches these via direct URL pasted into the application form. Clears blockers 1 + 2 of `docs/LINKEDIN_MDP_APPLICATION.md`.

2026-05-16 — fix(linkedin): clearer error when LinkedIn returns 403 on a company-page post. Team member hit "Accessing the resource is forbidden" from LinkedIn after the org-routing fix above — root cause is LinkedIn's documented restriction: the `w_organization_social` scope is granted at OAuth time but silently degraded until the LinkedIn app has Marketing Developer Platform partner approval. Previous error mapper in `/api/linkedin/publish` only caught 422 and 400-with-"organization permission"; 403 fell through to the raw LinkedIn message. New shared `mapLinkedInPublishError` helper in `src/lib/linkedin/publishError.ts` catches 401/403/422 + 400-with-permission-keywords for corporate posts and returns one actionable message branched on `isTeamMemberCorp`: explains MDP is the gate, says scheduling won't help (cron uses the same path and will also fail), points to the personal-posting workaround, and links to `linkedin.com/developers/apps`. Wired into all three publish paths: `/api/linkedin/publish`, `/api/posts/publish-now`, and `/api/cron/publish-due` (so the `failed_reason` shown in the dashboard for scheduled-then-failed posts is the same actionable message). Server-side logs the raw LinkedIn response + segment + team-member flag for debugging. Two UI corrections to match reality: preview-page warning no longer claims "use Schedule — once approved, scheduled posts will publish automatically" (true but misleading: scheduling fails until approval too, just silently to the dashboard); updated to be explicit that both paths are gated on MDP. Workspace health card on `/dashboard/settings/team` gets a footnote that even with green LinkedIn + green plan, company-page posts will fail until LinkedIn approves the owner's app — so members understand it's a separate, non-app-fixable gate.

2026-05-16 — feat(team): owner-health card on the member's `/dashboard/settings/team` view. Members can now see whether the team owner's LinkedIn token and plan are healthy so they can ping the owner out-of-band before posts start failing. `GET /api/team/members` now adds an `ownerHealth` object to the member-branch response with two derived states: `linkedin` (`healthy` | `warning` <14 days | `expired` | `disconnected`, plus `daysLeft` and `refreshExpiresAt` — keyed on `refresh_expires_at` since the 60-day access token auto-refreshes silently as long as refresh is valid) and `plan` (`healthy` for `active`/`trial`, `warning` otherwise — `renewsAt` only filled when unhealthy to keep the green-path UI clean). One extra Firestore read for the token + a conditional read for the subscription only when plan is unhealthy. Member surfaces it as a new "Workspace health" card between the owner info and Leave panel; healthy state still shows the card with green dots, warning/expired states render amber/red with copy that points the member back to the owner ("Ask {ownerName} to reconnect LinkedIn from Settings → Identity"). Informational only — no notifications or email; members ping the owner via whatever channel they already use. Owner view, solo/upsell view, and `/api/team/members` for owners are all untouched.

2026-05-16 — fix(team): team-member "Post Now" to company page was failing with "LinkedIn Organization ID not set" even after the owner set it — root cause was `/api/linkedin/publish` (the immediate-publish path used by the preview page, drafts editor, and history repost) had no team awareness. It read the orgId from the request body (sourced client-side from the member's own empty profile) and used the member's own LinkedIn token (which has no admin on the company page). `/api/cron/publish-due` and `/api/posts/publish-now` already handle this correctly via `post.user_id = ownerUid`. Fix: `/api/linkedin/publish` now runs `getActiveMembership(callerUid)` for corporate posts; when the caller is an active team member, it swaps the LinkedIn token to the team owner's (`tokens/{ownerUid}`), resolves the orgId from the owner's profile (`profiles/{ownerUid}.corporate.linkedinOrganizationId`), and runs the plan gate against the owner. Memory save also keys on the effective owner UID so company-page voice pools across the team — mirrors cron/publish-now behavior. Surfacing fix: `/api/user/profile` now also returns a `teamOwnerCorporate` field (name + orgId) for active members, and the preview page reads from it when `segment === "corporate"` so the "Profile name missing" gate disappears for team members and the right org name renders in the "Posting as Company Page" banner. Drafts editor + history repost paths needed no frontend change — they pass through the same `/api/linkedin/publish` route and inherit the server-side resolution.

2026-05-16 — fix(linkedin): bump `LI_VERSION` from `202505` to `202604` across `linkedin/publish`, `linkedin/engagement`, `cron/publish-due`, `engagement/sync`, and `posts/publish-now`. LinkedIn deprecated the May 2025 version (error: "requested version 20250501 is not active") since their versions sunset after ~12 months. Debug route left at its own version since it's a diagnostic.

2026-05-13 — fix(team): address major review findings. Six follow-up fixes from the second review pass. (1) `/api/team/invite/accept` now falls back to `users/{uid}.email` when the Firebase ID token JWT lacks an `email` claim — some auth providers (phone, certain SSO flows) strip it, and those users were hitting a confusing "different email address" rejection on otherwise-valid invites. (2) Acceptance transaction now re-reads invite status, team existence, and seat cap inside the txn — closes a race where two concurrent accepts could both land on the last seat, or an accept could succeed against an invite the owner had just revoked. (3) Pre-flight check that the inviting owner still has Business entitlement at the moment of acceptance — if owner downgraded between invite-send and accept, 409s the invitee instead of silently adding them to a now-illegal team. (4) Stopped overwriting the invitee's `plan` and `planStatus` to "free" on accept — `activeTeamId` alone drives `getUserPlan() → "pro"` per the Phase 2 design, and the prior write destroyed underlying plan state so leaving the team couldn't revert correctly. `subscriptionId` is now only cleared after a confirmed Razorpay cancel; on cancel failure the route returns 502 instead of silently membership'ing the user while their sub keeps billing. (5) `POST /api/posts` only stamps `authorDisplayName` when the drafter is a team member (`uid !== postOwnerUid`) — previously every corporate post including the owner's own solo posts got a redundant "Posted by {owner}" tag. (6) `/api/team/members` GET surfaces the Business seat cap when caller owns a team, even if their plan resolution is transient/lagging (e.g. Razorpay webhook just landed) — fixes "0 of 0 seats" flicker in the settings UI.

2026-05-13 — fix(team): address critical review findings. First-pass review fixes on the team feature shipped 2026-05-12. (1) `/api/team/invite` seat-cap + duplicate-email checks now run inside a single Firestore transaction — previously two simultaneous invites could both pass the read-then-write cap check and land a 6th seat. (2) Replaced composite `where(teamId) + where(email)` query on `teamInvites` with a single `where(teamId)` read + JS filter, per CLAUDE.md hard rule banning composite-index queries (the original code was importing `countActiveMembers`/`countPendingInvites` and would have required a composite index — both imports removed). (3) Seat-limit gate decoupled from owner's transient plan state — always honour the Business seat cap when the owner has an existing team, since the cancel-with-members guard (409 TEAM_HAS_MEMBERS) already prevents the inconsistent case upstream. (4) Added `INVITE_SIGNING_SECRET` to `.env.example` with a hard-requirement note — it was undocumented and missing this var 500s every `/api/team/*` call. (5) Defensive: `/api/team/invites/pending` now returns an empty list on any failure instead of bubbling a 500, so a misconfigured secret can't take down the whole dashboard shell via the invite banner.

2026-05-12 — docs(team): Phase 5 — Team feature documented end-to-end. `.claude/CLAUDE.md` plan table gains a Team column (Free/Starter/Pro = —, Business = "1 admin + 5 members"); gates list extended with `canUseTeam()` + `getTeamSeatLimit()`; new note that `getUserPlan()` returns "pro" automatically when `users/{uid}.activeTeamId` is set; key files adds `src/lib/team.ts`; routes table gets `/api/team/*`; new "Team (Business plan only)" rules section covers: 1 owner + 5 members cap, member-drafted corp post field shape (user_id=owner, authorUid=drafter, teamId), one-team-at-a-time + auto-cancel on accept, activeTeamId truth source, authorUid reassignment on leave/remove, 409 TEAM_HAS_MEMBERS block on owner downgrade, in-app invite surface (no email required). `NEEL_DOCS.md` gains a full Team Feature section: data model (teams/teamMembers/teamInvites/users/posts), HMAC invite-token format, plan-inheritance mechanic, post-pipeline routing logic (resolveTeamContext + canEditPost), publish path (cron untouched because user_id already routes correctly), accept/leave/remove flow with the shared `removeMember` helper, downgrade-block rationale, in-app invitee surface, and a complete file inventory (new vs modified).

2026-05-12 — feat(team): Phase 4 — leave + remove + author reassignment. New shared `removeMember(teamId, memberUid)` helper in `src/lib/team.ts` runs the full removal in one Firestore transaction (flip `teamMembers/{id}.status` to "removed", decrement `teams/{teamId}.memberCount`, FieldValue.delete() the user's `activeTeamId` which makes `getUserPlan` revert them to their actual plan on the next call) then chases it with a batch update over `posts where teamId == X` JS-filtered by `authorUid === memberUid AND segment === "corporate"` — setting `originalAuthorUid = authorUid` (history) and `authorUid = ownerUid` (so removed member loses PATCH/DELETE access via the canEditPost check); `authorDisplayName` left untouched so "Posted by" UI still shows the original drafter. Idempotent (no-op if already removed). New routes: `POST /api/team/members/remove` (owner-only, body `{memberUid}`, blocks self-remove with a hint to use /leave) and `POST /api/team/leave` (caller's own membership, no body). `/api/team/members` GET extended — when caller doesn't own a team but is an active member of someone else's, returns a `membership` object with `ownerUid`, `ownerName`, `ownerEmail`, `joinedAt` so the UI can render a leave panel. Team settings page now branches three ways: owner view (existing seat counter + invite + member list), member view (NEW — owner info card + "Leave team" red-button card explaining the consequences), or upsell (Free/Starter/Pro). Personal workspace data of removed members untouched per spec.

2026-05-12 — feat(team): Phase 3 — Team UI surfaces. New `src/app/dashboard/settings/team/page.tsx` is the owner's control surface: seat counter (X of 5 used), invite-by-email form, member list with avatars + remove buttons, pending-invites list with revoke buttons, and an inline "share this link" panel that surfaces the freshly-minted invite URL (with copy-to-clipboard) so the owner can hand-deliver the link via whatever channel they want while email is dormant. Non-Business plans see an upsell card pointing to billing instead. Sidebar `Account` group gets a new "Team" link (`Users` icon) under Profile so both owner and member can find it; the member's view is on the same page and currently shows nothing (members will get a leave-team panel in Phase 4 — for now the in-app banner is their primary surface). `PricingCards.tsx` Business plan adds "1 admin + 5 team members" as a bullet; Pro left untouched (already didn't mention team). `PostDetailDrawer.tsx` shows a Cridl-blue "Posted by {name}" pill next to the existing segment pill whenever the post is corporate AND has an `authorDisplayName` field — so owners can see who on the team drafted each company-page post.

2026-05-12 — feat(team): Phase 2 — post pipeline + permissions live for team posts. `getUserPlan()` in `src/lib/checkSubscription.ts` now returns "pro" automatically whenever `users/{uid}.activeTeamId` is set (and base plan isn't already business) — single zero-read-cost change that gives every existing plan-gated route (generation, image, face, carousel, campaigns) the correct effective plan for team members without touching individual call sites. `/api/posts` rewritten: new `resolveTeamContext(uid)` returns owner|member|none in one Firestore read pair, then POST routes corporate drafts based on role — members write the post with `user_id = team.ownerUid` (so the existing cron worker uses the owner's LinkedIn token unmodified, and usage tracking auto-pools against owner) while stamping `teamId` + `authorUid` + `authorDisplayName` for the shared queue + "Posted by" UI; owners get `user_id = self` plus `teamId` so members can see their drafts; solo Pro/Business users keep their existing flow with `authorUid = self`. GET now does a second `where(teamId == activeTeamId)` query for active members and dedupes against their own posts (single-where each, no composite index). PATCH and DELETE replaced their `user_id === uid` checks with `canEditPost(data, uid)` which permits `user_id === uid OR authorUid === uid` — covers both owner (always satisfies user_id) and drafter (satisfies authorUid). `/api/posts/publish-now` makes the same auth flip and now fetches `tokens/{post.user_id}` instead of `tokens/{caller_uid}` — auto-routes team-drafted corporate posts through the owner's company-page-authorized LinkedIn token; plan gate on corporate also moved to checking the post owner's plan (always Business for team posts), not the caller's. `/api/subscriptions/cancel` hard-blocks cancellation when caller owns a team with active members — returns 409 + `code: "TEAM_HAS_MEMBERS"` so the billing UI can show a clear message ("remove all team members first"). Removed now-redundant `getEffectivePlan()` from `src/lib/team.ts` (getUserPlan does it). Cron worker untouched — already keys on `post.user_id` for both token and orgId lookup, so team posts route correctly with zero changes.

2026-05-12 — feat(team): Phase 1.5 — drop email path, surface invites in-app. Resend send call removed from `POST /api/team/invite`; the invite doc now stores `inviterName` + `orgName` so the invitee can see who invited them without an extra lookup. New `GET /api/team/invites/pending` route returns any pending invites for the current user (matched by Firebase Auth email, expiry-filtered) with a freshly-minted token for each. New `GET /api/team/invite/[token]/preview` returns the full invite + team meta (owner name, org, current active member count, whether invitee's signed-in email matches, whether expired) so the accept page can render details before the user commits. `/invite/[token]/page.tsx` rewritten — fetches preview on mount, shows a summary card (signed-in-as, invite-addressed-to, team size after joining, expiry date), disables Accept when expired or email mismatch, and explains all three consequences (company-page access, Pro features, paid-sub auto-cancel) before the button. New `PendingInviteBanner` component polls `/api/team/invites/pending` once per dashboard mount and surfaces a Cridl-blue banner at the top of the main content area (both desktop and mobile shells of `dashboard/layout.tsx`) with inviter name, team label, a "Review" CTA that links to the invite page, and a per-session dismiss. Resend package and `src/lib/email/resend.ts` left in place dormant — easy to re-enable in the future. `RESEND_API_KEY` / `RESEND_FROM_EMAIL` env vars no longer required; `INVITE_SIGNING_SECRET` still required.

2026-05-12 — feat(team): Phase 1 of team feature — data model + invite flow. Business plan now exposes a `teamSize: 5` field in `PLAN_LIMITS` (Free/Starter/Pro stay at 0); new gates `canUseTeam()` and `getTeamSeatLimit()` in `src/lib/checkSubscription.ts`. New collections: `teams/{teamId}` (one per owner, lazily created on first invite, denormalized `memberCount`), `teamMembers/{teamId}_{memberUid}` (composite docId so re-invite re-activates the same record with full history intact), `teamInvites/{inviteId}` (signed-token invites, 7-day expiry, status: pending/accepted/expired/revoked). New `src/lib/team.ts` with helpers `ensureTeamForOwner`, `getActiveMembership`, `getEffectivePlan` (returns Pro-tier for active members of Business teams), HMAC-signed invite token generation/verification (single `INVITE_SIGNING_SECRET` env), and seat-counting utilities that walk Firestore directly (single-where + JS filter per project convention — no composite indexes). New routes (all node runtime, Bearer auth): `POST /api/team/invite` (lazy-creates team, validates plan, checks seat + pending count, blocks self-invite + duplicate pending, sends Resend email + returns inviteUrl so owner can copy/paste if email fails), `POST /api/team/invite/accept` (verifies token signature, expiry, email match against decoded Firebase ID token email; blocks if invitee already owns or belongs to another team; auto-cancels invitee's active Razorpay sub via `subscriptions.cancel({cancel_at_cycle_end:false})` and wipes their `subscriptionId`/`planStatus` — failure logs but proceeds; runs the membership write + team `memberCount` increment + invite status flip + user `activeTeamId` in a single Firestore transaction), `POST /api/team/invite/revoke` (owner-only, pending-only), `GET /api/team/members` (returns team meta + active members + pending invites + seat limit). New `src/lib/email/resend.ts` wraps the Resend SDK with a minimal HTML+text invite template; reads `RESEND_API_KEY`, `RESEND_FROM_EMAIL`, `NEXT_PUBLIC_APP_URL`. Invite-accept page at `/invite/[token]/page.tsx` — gates on `useAuth()`, redirects to `/login?redirect=...` if unauthenticated (login page already supports the param), shows what acceptance will do (join team, get Pro features, cancel any paid sub) and an Accept button that posts to the API. Phase 1 ships dormant: nothing in the existing post pipeline checks team membership yet (that's Phase 2). New env vars to set in Vercel: `RESEND_API_KEY`, `RESEND_FROM_EMAIL` (e.g. `Cridl <invites@cridl.app>`), `INVITE_SIGNING_SECRET` (any 32+ char random string). `resend` npm package installed.

2026-05-11 — fix(generate): HTTP 504 on long Cortex generations. Root cause: Vercel Edge gateway kills any request that doesn't return its first byte within 25s. Stage 1 (Sonnet post) at 16s + Stage 2 (Gemini image prompt) at 12s = up to 28s wall time, which blew the budget once recent prompt growth pushed Sonnet response time near the cap. Two-part fix: (1) `/api/ai/generate` now returns a streaming NDJSON Response — opens with a `{type:"start"}` frame immediately and emits `{type:"ping"}` every 4s while the model runs, so the gateway never times out (Vercel allows up to 300s once first byte is sent); final post arrives as a `{type:"result"}` frame, errors as `{type:"error"}`, completion as `{type:"done"}`. Quota refund still fires on every failure path including mid-stream client abort. Primary Sonnet timeout bumped 16s→35s and fallback Gemini 12s→20s now that the gateway is no longer the ceiling. (2) Stage 2 (image prompt) extracted from `generatePost()` — the route now does post-only work, halving wall time; the client lazy-loads the image prompt via the existing `/api/ai/image-prompt` route in parallel with navigation (12s soft race), and the preview page has a `useEffect` fallback that re-fetches on mount if it's still missing. New helper `src/lib/utils/consumeNdjson.ts` parses the NDJSON stream. Both call sites updated: `dashboard/create/page.tsx` (initial gen) and `dashboard/create/preview/page.tsx` (regen). Campaigns route uses its own `callOpenRouter` and is unaffected.

2026-05-09 — feat(quality): three-part Cortex hardening. (1) X-isn't-Y reveal ban promoted from rule 5 of COPYWRITING_RULES (where it was buried in a 16-item list and ignored in 2/5 scored posts) to a hard zero ban in `OUTPUT_RULES` with explicit variant coverage and pre-output scan instruction. (2) New `src/lib/ai/currentContext.ts` with `getCurrentDateContext()` + `withDateContext(messages)` helper, wired into 13 LLM call sites across 11 files (writer, research, angle engine, voice-dna, rewrite, memory-extract, idea, score, onboarding, campaigns, best-time, profile-enhance, style-dna). Fixes user-reported bug where a generated post referenced "the shift in 2024" — model defaulted to its training-cutoff year because no current date was passed in. Image-vision routes skipped (multimodal content, date irrelevant). (3) New `score-reports/PROMPT_CHANGELOG.md` — append-only institutional memory of every prompt change, structured around a load-bearing "Do NOT revert because" field that names the specific anti-pattern each rule prevents, so future editors don't silently undo improvements. Backfilled with prior two iterations. 🛑 banner added to top of `NEEL_RUNTIME.md` instructing editors to read the changelog before editing.

2026-05-09 — feat(quality): zero em-dash ban. User feedback — em dashes (—) are an AI fingerprint; user's natural keyboard hyphen is shorter, so any em dash in output reads as machine-written. Changed prompt rule from "max 2 per post" to a hard ZERO ban in both `NEEL_RUNTIME.md` and `src/lib/ai/neel-prompt-sections.ts`: added explicit ZERO EM DASHES line at the top of `OUTPUT_RULES` (the non-negotiable section that runs before everything else), instructing Cortex to use commas, periods, colons, parentheses, or two short sentences instead, and to count before output. Also tightened the matching `AVOID` bullet from "Max 2 per post" to "ZERO per post. Hard ban." Scorer heuristic in `scripts/score-post.mjs` updated to match: rule renamed `max_two_em_dashes` → `zero_em_dashes`, threshold flipped from `<=2` to `===0`, severity raised from `low` → `high` so any em dash now meaningfully drops the structure score. Baseline (commit a75a2f3) had 3/4 posts violating the old max-2 rule — this run will recalibrate against zero-tolerance.

2026-05-09 — tools(quality): `/score-post` slash command + hybrid post-quality scorer for the prompt-iteration loop. Goal: replace eyeballed quality reviews with deterministic scoring tagged to the prompt commit SHA, so prompt edits can be measured against each other across runs. New files: (1) `scripts/score-post.mjs` — hybrid scorer combining 15 deterministic regex/string heuristics (round-percent fabrication, hashtags, "I keep seeing" crutch, "X isn't Y. It's Z." reveal pattern, em-dash count, exclamations, banned corporate verbs, cliché openings, engagement-bait closers, filler transitions, numbered lessons, labeled IMPERFECTION, fake-pivot tells, word count, tricolon fragments) with a 6-dimension LLM judge (Gemini Flash via OpenRouter — scores specific_subject, inline_doubt, uncomfortable_truth, asymmetric_structure, first_person_conversational, plain_verbs on 0–10). Combined into 0–100 across 5 weighted categories: Specificity / Voice / Structure / Honesty / AI-tells (each /20). Verdict bands: 🟢 85+ publish-ready, 🟡 70–84 polish, 🟠 55–69 weak, 🔴 <55 fail. (2) `.claude/commands/score-post.md` — slash-command wrapper with three modes: no-args (regenerates 5 standard test topics via `scripts/test-cortex.mjs` and scores each), `--file <path>` (scores a file), or pasted text (scores inline). (3) `scripts/test-cortex.mjs` — standalone Cortex generator that bypasses Firebase/auth/quota and reads `neel-prompt-sections.ts` at runtime so it always uses the latest local prompt (no redeploy needed for testing). (4) Reports written to `score-reports/SCORE_<date>_<time>_<sha>.md` — tagged with current git short-SHA so iteration progress is auditable across runs. First baseline run on commit `a75a2f3` averaged 78.9/100 (🟡); top failure modes: corporate-verb leakage ("optimize" in 3/4), em-dash overuse, residual "X isn't Y. It's Z." pattern. No production code touched.

2026-05-09 — feat(quality): human-feel pass on Cortex prompt — 16-rule rewrite of `COPYWRITING_RULES`, expanded `AVOID` list, tightened `STRUCTURE` and `SEGMENT_INDIVIDUAL`. Driven by audit of 5 fresh test outputs that all read as AI-slop despite the prior thinking-first changes. Root causes identified: (1) fabricated stats in every post (40%, 60–70%, 75%, 68%, 42% — all invented by the model), (2) "I keep seeing" became a universal opener crutch, (3) tacked-on standalone IMPERFECTION line ("I'm still figuring out X"), (4) "X isn't Y. It's Z." reveal closer in every post (the most overused LinkedIn AI pattern), (5) trailing hashtags, (6) identical skeleton across all 5 posts, (7) plural abstractions ("founders," "marketers," "teams") instead of specific anonymized subjects. New rules: STAT INTEGRITY (hard ban on invented numbers — only research-passed stats allowed); SPECIFICITY OVER ABSTRACTION (mandate one anonymized subject with concrete attribute per post); BAN "I keep seeing" / "I've been seeing" as opener anchor; BAN "X isn't Y. It's Z." closer; BAN trailing hashtags entirely (was 0–3, now 0); INLINE DOUBT ONLY (no labeled "I'm still figuring out" paragraphs); ROTATE THE CLOSE (5 options: question / flat statement / admission / offer / no CTA); SHOW WORK DON'T NUMBER LESSONS; PLAIN VERBS list (use/find/push/grow vs leverage/unlock/drive/scale); FRAGMENTS ALLOWED (max 2, never 3 in a row); ASYMMETRIC STRUCTURE; ONE UNCOMFORTABLE TRUTH per post; expanded AVOID with fake-pivot tells ("Here's what's actually happening" / "Here's the thing"), tricolons of fragments, numbered lessons. `STRUCTURE` CTA section rewritten to mandate rotation; `SEGMENT_INDIVIDUAL` removed "I keep seeing..." as the safe-anchor fallback (replaced with "specific recent moment" requirement). Synced to `src/lib/ai/neel-prompt-sections.ts`. No TS/architecture changes.

2026-05-09 — fix(style-dna+research): three post-review bug fixes. (1) CRITICAL: style-dna Firestore write now uses dot-notation `{ [\`${segment}.style_dna\`]: styleDna }` — prior nested-map write with `merge:true` replaced the entire segment map, silently wiping name/bio/brandVoice and all other profile fields for any user who uploaded writing samples. (2) Angle engine in `research.ts` server-action path now wrapped in `Promise.race()` with 5s timeout — edge route already had AbortController timeout but server action had none, risking indefinite hang on OpenRouter stall. (3) Dead `dominant` variable removed from `buildMemoryBlock()` in `generate.ts`.

2026-05-09 — feat(quality): thinking-first architecture — Angle Engine, Style DNA, Hook Diversity. Phase 1 (Angle Engine): second LLM call added in research step (both server-action + edge route) after research JSON is obtained; generates 3 hook candidates (stat/story/contrarian) + recommended angle + hook type label; `ResearchResult` now carries `hookCandidates`, `recommendedAngle`, `recommendedHookType`; `generate.ts` injects ANGLE ENGINE block into system prompt between FORMATTING and BRAND CONTEXT; edge budget maintained — angle call has 5s timeout, worst-case total 12+10+5=27s < 30s limit; graceful — failure leaves generation unchanged. Phase 2 (Style DNA): new `StyleDNA` interface in `profiles.ts`; new `/api/profiles/style-dna` route extracts compact writing fingerprint (~250 chars) from user-upload samples via LLM and saves to `profiles/{uid}` with deep-merge; `memory/upload` fires-and-forgets extraction after samples are added; `generate.ts` injects compact DNA block when available, falls back to raw samples for users without DNA — 10x token reduction with no loss of actionable voice signals. Phase 3 (Hook Diversity): new `detectHookType()` in `lib/ai/detectHookType.ts` — pure regex, no LLM, edge-safe; `memory/save` route stores `hook_type` on every auto memory entry; `buildMemoryBlock()` shows HOOK VARIETY hint when ≥3 entries have hook_type data, suggesting underused types.

2026-05-09 — refactor(reliability+latency+quality): architectural refactor across 3 priorities. (1) RELIABILITY: post quota now refunded on generation failure (was charged but never returned on model error/timeout/safety); image quota now refunded on submit failure (was only refunded on poll-FAILED); face-generate converted from synchronous fal.run (15–25s, Vercel timeout risk) to queue submit+poll architecture matching /api/image/generate — POST returns {request_id}, GET polls status, FAILED auto-refunds faceImage quota; alert() in source extraction replaced with setGenerateError for consistent UX. (2) LATENCY: research + memory fetch parallelised with Promise.all in create/page.tsx (saves ~3–5s of sequential wait); auto voice-rewrite pass (Stage 1.5) removed — added ~10s to every generation and is superseded by voice signals already in the main prompt (writing samples, style_notes, memory); max_tokens now varies by post length: short=800, medium=1200, long=1600 (prevents truncation on 400-word posts). (3) QUALITY: final post generation (Stage 1 only) switched from google/gemini-2.5-flash to anthropic/claude-sonnet-4-6 via OpenRouter — superior hook quality, emotional realism, sentence-rhythm variation; all other tasks (research, image prompts, image hook, scoring, carousel, rewrite-on-demand) stay on Gemini Flash. Smarter retry: safety/content_filter failures no longer retried (same model would refuse again); timeout + transient errors still retry. Structured timing logs added: [Cortex trace=<id>] stage=post/image-prompt ms=<n>.

2026-05-08 — fix(research): align edge research route personal-intent stripping with server-action. `jtbd` and `customerPains` were being stripped in `src/lib/ai/research.ts` (server action) but NOT in `src/app/api/ai/research/route.ts` (edge route). Both now gate the same 5 fields behind `isProfessional`: niche, bioOrOffering, icp, jtbd, customerPains.
2026-05-08 — fix(generate): plug remaining brand-context bleed on personal posts. Even after intent classified correctly as "personal", `clientProfile.verbatimLanguage` ("Native phrases to weave in naturally") and `clientProfile.personality` were still injected into the system prompt — and for users whose brand IS a LinkedIn-posting tool, those fields contain phrases like "I never get time to post" / "I have ideas but can't convert them to posts". Cortex obediently wove them into a Citadel personal post. Gated both fields behind `isProfessional` so on personal intent only `name` + `roleOrIndustry` + `wordsToAvoid` survive (negative constraints can't bleed in as content). Also stripped `summary` + `keywords` from writing-samples on personal intent — only `style_notes` survives. Added a 7th rule to the personal-top-override making the bleed pattern explicit ("if any phrase in the brand profile references LinkedIn / posting / your product, IGNORE it for this post").
2026-05-08 — fix(intent): broaden personal-topic detection regex (gerund forms) + de-duplicate. Topics like "Watching Citadel — AI in spy networks" were classified as `professional` because the regex only matched past-tense `/\bwatched\b/`. With brand context flagged "non-negotiable", Cortex was opening posts with the user's own customer-pain (e.g. "I never get time to post on LinkedIn") before pivoting to the actual topic. Extracted shared `src/lib/ai/intent.ts` (edge-safe, pure) — now the single source of truth for both `src/lib/ai/research.ts` (server action) and `src/app/api/ai/research/route.ts` (edge route). Added gerund forms (`watching`, `seeing`, `listening`, `reading`); kept conservative on bare verbs (excluded plain `see`/`show`/`series` to avoid false-positives like "see growth", "webinar series"). Verified with 10 representative cases — Citadel correctly classifies as personal; "see ROI", "webinar series", "show growth" stay professional.
2026-05-07 — feat(images): rewrote `IMAGE_PROMPT_SYSTEM` + `IMAGE_PROMPT_USER` for dense 5-zone editorial infographic. Old spec capped in-image text at 30 words and produced sparse single-hero layouts; new spec extracts 10 strings from the post (headline, subhead, section label, 3 pattern labels + sublabels, quote, reaction, stat + context + takeaway, question) and renders them across 5 stacked zones — Headline · Pattern Strip · Quote Card · Stat Band · Engagement Question. Text budget 60–75 words. Palette locked to `#0B1220` navy + `#FFFFFF` white + `#9CA3AF` gray + `#0A66C2` LinkedIn blue (no orange/green/light-mode fallback). Hard bans on people, mascots, logos, watermarks, gradients, AI-cliché motifs. Synced to `src/lib/ai/neel-prompt-sections.ts`. NEEL_DOCS.md Layer G rewritten to v2.0.
2026-05-07 — feat(generate): voice-rewrite pass now runs on every post generation by default. After the main Cortex post is drafted, the same prompt powering the manual "Rewrite in my voice" button (`REWRITE_IN_VOICE_PROMPT`) re-runs as Stage 1.5 with the user's profile + writing samples — strips AI-slop tells (jargon, "In today's digital age", excessive em-dashes) and locks the post to the author's voice. New helper `src/lib/ai/rewriteInVoice.ts` (silent-fallback to draft if call fails or output too short). Wired into `generatePost()` and the Pro+ campaign generator. Standalone `/api/posts/rewrite` route refactored to use the same helper. ~5–8s added per generation; no extra usage charge (counted as 1 post like before). Manual `RewriteButton` retained for editing pasted/edited text.
2026-05-07 — refactor(settings): hide AI tab from Profile (model + system prompt). Single backend model (Gemini 2.5 Flash) and base Cortex prompt are final — users can't change them. Per-user `systemPrompt` no longer applied to generation; "AI Writer" row removed from Create page sidebar. Schema fields kept (no Firestore migration), just inert.
2026-05-07 — feat(ideas): archived filter tab + Restore action on archived cards. Archive no longer makes ideas disappear with no way back; "All" hides archived (use the Archived tab to surface them).
2026-05-07 — refactor(brand): replaced gradient Zap+text logo with `Logo` wordmark component ("crid" in foreground + "l" in #2563eb). Theme-aware via `var(--foreground)` — works in light + dark without per-theme assets.
2026-05-07 — fix(generate): surface real upstream error message from `/api/ai/generate` instead of opaque "Generation failed". Create page parses JSON error body. Users now see the actual cause (truncation / safety filter / quota) and we can diagnose without server logs.
2026-05-07 — feat(images): switch image generation to gpt-image-2 (medium, 1024×1024). New SaaS-infographic prompt for single + carousel (text rendered inside image). Removed image-style picker (settings tab + per-segment field). Face-gen still uses prior fal model.
2026-05-07 — fix(images): single-image prompt actually switched to SaaS-infographic. The 2026-05-07 entry above only updated the carousel prompt — IMAGE_PROMPT_SYSTEM still emitted "cinematic photography" prompts, so single-image generation kept producing moody silhouette photos. Replaced with infographic spec: bold quoted headline, 2–4 flat-icon blocks, accent callout, CTA pill, dark/light mode lock, banned cinematic/photoreal keywords.
2026-05-07 — refactor(images): extracted submit+poll loop to `src/lib/ai/clientImage.ts` (was duplicated in 4 places). Added `refundUsage` + `/api/usage/refund` so the image-poll route auto-refunds quota when fal.ai returns FAILED — users no longer charged for jobs that never produced an image.

2026-04-26 — Merged 5 agents into 2 (Builder, Strategist).
2026-04-26 — Renamed `Master_Neel_Prompt.md` → `NEEL_RUNTIME.md`, `NEEL.md` → `NEEL_DOCS.md`. Updated all refs.
2026-04-26 — Tightened `CLAUDE.md` to operational memory.
2026-04-26 — Added `CHANGES.md`.
2026-04-26 — Compressed Cortex prompt 44% (26,763 → 14,830 bytes). Deduped overlapping rules across OUTPUT/COPYWRITING/AI_SLOP/FORMATTING. No rule loss. `NEEL_RUNTIME.md` + `neel-prompt-sections.ts` synced.
2026-04-26 — Added Read deny rules for `NEEL_DOCS.md` + `CHANGES.md` (human-only files).

2026-04-28: feat(quality): Post Quality Score (gauge + suggestions + apply-hook).

2026-05-07: fix(create): Idea Bank topic no longer overwritten by stale localStorage draft on Generate.
2026-05-07: fix(pwa): disable aggressive precaching, NetworkFirst for navigations, cleanupOutdatedCaches + skipWaiting — fixes stale UI for returning users.

2026-04-30: fix(dashboard): show LinkedIn/profile photo in Personal + Company banner & System Status (fallback chain, onError hide).
2026-04-30: feat(ai): add `moonshotai/kimi-k2.6` to AI model picker.
2026-04-30: fix(dashboard/stats): auto-refresh LinkedIn picture URL from /userinfo every 24h (licdn signed URLs expire); persist back to Firestore — fixes blank avatar on dashboard.
2026-04-30: fix(score): use Kimi K2.6 for quality scoring; Apply hook shows "Applied ✓".

2026-05-01: feat(regen): temporary regeneration memory — Firestore `regeneration_sessions/{sessionId}` holds initial post (anchor) + each regen turn (text + user comment). Prompt now includes anchor + last 2 turns + elided count to prevent drift across iterations. Capped at 10 turns. Auto-cleaned on save/schedule/publish.
2026-04-30: fix(score): revert scorer to Gemini 2.0 Flash (Kimi returned unparseable JSON); add gpt-4o-mini fallback; harden parsing; system+user message split for stricter JSON; recompute score from breakdown if model returns 0.
2026-04-29: fix(tokens/save): allow through when INTERNAL_API_SECRET unset — was 500'ing silently and breaking new-user LinkedIn connect (UI showed connected but Firestore tokens never saved).
2026-04-29: feat(rewrite): Rewrite In My Voice (modal + side-by-side + score) on /create + /preview; gated as 1 usage.

2026-05-01: feat(ai): Kimi K2.6 is now primary across every text-LLM call site for both Individual and Corporate — post generation (`generate.ts`), Cortex post writer + image prompt + image hook, research (route + lib), post quality scoring, Rewrite In My Voice (via `DEFAULT_MODEL`), profile extraction (via `DEFAULT_MODEL`), Idea Bank, Voice DNA, Profile "Edit with AI" enhance, Best-Time scheduler, Campaigns (research + sequential post gen + image prompt), and `profile/init` default `primaryModel`. Gemini 2.0 Flash is the automatic fallback on HTTP error / unparseable JSON / empty output (Kimi previously failed JSON parsing on scoring — every site that requires JSON now retries Gemini). Vision-only routes (`extract-context` image analysis, `validate-face`) stay on Gemini/gpt-4o-mini since Kimi K2.6 is text-only.

2026-05-01: refactor(ai): single-model consolidation — `google/gemini-2.5-flash` is now the sole LLM across all of Cridl. Removed Kimi K2.6 + Gemini 2.0 Flash + gpt-4o-mini from every text call site (15 files: `openrouter.ts`, `generate.ts`, research route + lib, scoring, idea-generate, voice-dna, profile-enhance, best-time, campaigns generate, memory-extract, profile/init, create page, settings page, gemini.ts) and from vision routes (`extract-context`, `validate-face`). Reason: Kimi caused `FUNCTION_INVOCATION_TIMEOUT` on edge (verbose JSON tripping max_tokens, no SDK timeout default) and unparseable JSON on scoring. Each call now wrapped in AbortController (12s primary + 10s retry on same model) so a stalled call can never exceed the edge function budget. Settings model picker reduced to a single option.
