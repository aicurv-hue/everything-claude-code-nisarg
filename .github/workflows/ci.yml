name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  check:
    name: CI Check
    runs-on: ubuntu-latest
    steps:
      - name: Pass
        run: echo "CI passed — Vercel handles production builds"
